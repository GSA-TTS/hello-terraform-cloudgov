## From hello-terraform-cloudgov/template/.gitignore
# Ignore Terraform state files and secrets
.terraform
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
secrets.*.tfvars

# Keep local terraform config here so people don't accidentally commit changes
terraform/local.auto.tfvars

# Ignore .zip files generated by Terraform during deployment to cloud.gov
terraform/dist
