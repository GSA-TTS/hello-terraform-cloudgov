cf_space_name   = "{{ cloud_gov_staging_space }}"
env             = "staging"
allow_space_ssh = true
# TODO: Can we loop and generate one per application?
# host_names must be unique across cloud.gov, default is "{{ '{{ application.name }}-${var.env}' }}"
host_name = null
space_developers = [
  # enter developer emails that should have ssh access to staging
]
